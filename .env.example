# Get this from your Neon project dashboard — Connection Details, use the
# "pooled connection" string (has -pooler in the hostname), not the direct one.
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/taskgrind?sslmode=require"

# Any long random string. Generate one with: openssl rand -base64 32
JWT_SECRET="replace-this-with-a-real-random-secret"
